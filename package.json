{
  "name": "shark-agent",
  "version": "4.8.3",
  "description": "SHARK Agent v4.8.3 - Triple-brain execution with mechanical gate enforcement. Execution Brain for autonomous BUILD + TEST operations.",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "exports": {
    ".": "./dist/index.js",
    "./*": "./dist/*.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm --bundle --external @opencode-ai/plugin",
    "build:standalone": "bun build src/index.ts --outdir dist --target bun --format esm --bundle",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^5.3.0",
    "zod": "^4.1.8"
  },
  "files": [
    "dist/",
    "src/",
    "package.json",
    "README.md"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "opencode",
    "agent",
    "shark",
    "triple-brain",
    "gate-enforcement",
    "execution-brain"
  ],
  "author": "Shark Agent",
  "license": "MIT"
}
